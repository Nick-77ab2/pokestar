/**
 * @file
 * @author Nick Pelletier
 * @date 2023
 * @section Description
 * 
 * gameSelectRow.js creates the gamecenter game selection.
*/
const { ActionRowBuilder, StringSelectMenuBuilder } = require("discord.js");
const { eventNames } = require("../config/eventConfig");

const buildGameSelectRow = (games, data, eventName ) =>{

}

module.exports = {
    buildGameSelectRow
};